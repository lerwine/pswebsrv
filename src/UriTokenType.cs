using System;

namespace Erwine.Leonard.T.PSWebSrv
{
    public enum UriTokenType
    {
        PathName,
        Scheme,
        UrlScheme,
        QueryStart,
        UserName,
        QueryKey,
        Fragment,
        HostName,
        PortNumber,
        QueryValue,
        Password
    }
}